'use client';

import ArticleTemplate from '@/components/ArticleTemplate';
import { pValuesConfig } from '@/content/articles/statistics/p-values';

export default function PValuesPage() {
  return <ArticleTemplate config={pValuesConfig} />;
}